<% if @event.errors.any? %>
  <section id='errors'>
  <header>
    <h2>Your event could not be saved.</h2>
      <ul>
        <% @event.errors.full_messages.each do |error| %>
            <li><%= error %></li>
        <% end %>
      </ul>
  </header>
  </section>
<% end %>