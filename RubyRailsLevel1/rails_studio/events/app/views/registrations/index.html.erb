<header>
	<h1> Registrations for <%= link_to(@event.name,@event) %></h1>
</header>

<ol id="registrations">
<% @registrations.each do |r| %>
	<li>
		<%= mail_to(r.email, r.name) %>
		<%= r.event.name %>
	</li>


<% end %>
</ol>